# Frontend

React, TypeScript, and Vite single-page application. During development Vite proxies relative `/api` requests to `localhost:8000`; in AWS the ALB owns that routing. Run `npm ci`, then `npm run dev`. Validation commands are `npm run lint`, `npm run typecheck`, `npm test`, and `npm run build`.
