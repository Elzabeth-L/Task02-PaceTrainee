# Backend

FastAPI service exposing `/api/health`, `/api/ready`, and `/api/info`. It emits structured JSON request logs and accepts or creates an `X-Request-ID`. Install the pinned development dependencies, then run `ruff check .`, `ruff format --check .`, `mypy app`, and `pytest`.
